# Flask Secret Keys - Generate your own using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here
WTF_CSRF_SECRET_KEY=your-csrf-secret-key-here

# Flask Environment (development or production)
FLASK_ENV=development

# Database Configuration
# For production use a managed database, e.g.
# DATABASE_URL=postgresql://user:password@hostname:5432/messdb
# For Supabase pooler: postgresql://postgres.projectref:password@aws-region.pooler.supabase.com:6543/postgres
DATABASE_URL=sqlite:///./instance/mess_management.db

# Optional: Force IPv4 connection (only needed if IPv6 connection fails)
# DATABASE_HOSTADDR=13.200.110.68

# UPI Payment Configuration (Optional - can be configured in Settings page)
UPI_ID=your-upi-id@provider
UPI_NAME=Your Mess Name
